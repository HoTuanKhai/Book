{% extends 'base.html.twig' %}

{% block body %}
    <div class="bs-example" data-example-id="striped-table">
        <table class="table table-striped">
            <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Book_id</th>
                <th>Staff_id</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for c in cate %}
                <tr>
                    <th scope="row">{{ c.id }}</th>
                    <td>{{ c.Type }}</td>
                    <td>{{ c.Book_id }}</td>
                    <td>{{ c.Staff_id }}</td>
                    <td>
                        {#<a href="{{ path('c_details', {'id': c.id}) }} " class='btn btn-success'>View</a>#}
                        {#<a href="{{ path('c_edit', {'id': c.id}) }}" class='btn btn-default'>Edit</a>#}
                        {#<a href="{{ path('c_delete', {'id': c.id}) }}" class='btn btn-danger'>Delete</a>#}
                        <a href="{{ path('c_details', {'id': c.id}) }} " class='btn btn-success'>View</a> 
                        <a href="#" class='btn btn-default'>Edit</a>
                        <a href="#" class='btn btn-danger'>Delete</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}